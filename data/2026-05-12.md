<div id=toc></div>

# Table of Contents

- [cs.PL](#cs.PL) [Total: 9]
- [cs.LG](#cs.LG) [Total: 1]
- [cs.AI](#cs.AI) [Total: 1]
- [cs.CR](#cs.CR) [Total: 1]
- [cs.MA](#cs.MA) [Total: 1]


<div id='cs.PL'></div>

# cs.PL [[Back]](#toc)

### [1] [GPU-Accelerated Synthesis of Mixed-Boolean Arithmetic: Beyond Caching](https://arxiv.org/abs/2605.08243)
*Gabriel Bathie,Baptiste Mouillon,Nathanaël Fijalkow*

Main category: cs.PL

TL;DR: SIMBA是一个免缓存的GPU加速MBA表达式合成器，通过自底向上的枚举设计实现大规模并行，相比现有方法在速度和规模上均有显著提升。


<details>
  <summary>Details</summary>
Motivation: 现有MBA表达式合成器基于CPU，处理大规模规格或复杂目标时扩展性差；GPU加速方法虽快但依赖缓存观察等价候选，而MBA输出的量化位向量行为空间巨大，缓存策略失效。

Method: 提出SIMBA，采用免缓存的自底向上枚举方法，针对GPU架构设计，保持工作局部性和高度并行性，避免语言缓存。

Result: SIMBA相比已有MBA合成工具速度显著更快，能处理更大规模的规格，达到现有方法无法求解的表达式规模。

Conclusion: 免缓存的GPU合成方法在量化域中是实用且可扩展的方案，可作为缓存中心设计的强有力替代。

Abstract: Synthesizing Mixed-Boolean Arithmetic (MBA) expressions from input-output examples is central to program deobfuscation and also useful for compiler optimization, reverse engineering, and cryptanalysis. Existing MBA synthesizers are typically CPU-based and scale poorly on large specifications or complex targets. Recent GPU-accelerated synthesis methods achieve large speedups in qualitative settings, but they depend on caching observationally equivalent candidates; this strategy breaks down for MBA because candidate outputs are quantitative bitvectors and the behavioral space is enormous. We present SIMBA (Synthesis of Mixed-Boolean Arithmetic), a GPU-accelerated MBA synthesizer built around cache-free bottom-up enumeration. SIMBA avoids language caches entirely and uses a GPU-oriented enumeration design that keeps work local and highly parallel. In experiments, SIMBA is substantially faster than prior MBA synthesis tools, handles larger specifications, and reaches expression sizes that existing methods fail to solve. These results establish cache-free GPU synthesis as a practical and scalable approach for quantitative domains, and identify it as a strong alternative to cache-centric designs.

</details>


### [2] [LLM Translation of Compiler Intermediate Representation](https://arxiv.org/abs/2605.08247)
*Andrea Valenzuela Ramirez,Cristian Gutierrez-Gomez,Marta Barroso,Dario Garcia-Gasulla,Sara Royuela*

Main category: cs.PL

TL;DR: 本文提出IRIS-14B，一个140亿参数的Transformer模型，用于将GCC的GIMPLE IR翻译为LLVM IR，在真实代码和竞赛编程问题上比现有最强开源模型（130亿至1万亿参数）准确率高出最多44个百分点，是首个专门为IR到IR翻译训练的模型。


<details>
  <summary>Details</summary>
Motivation: GCC和LLVM使用不同的中间表示（IR），导致跨工具链交互困难，限制了编译器前端、后端和优化流水线在不同编程语言和编译生态系统间的复用。传统基于规则的翻译器虽试图解决这一问题，但复杂性和维护成本阻碍了实际应用。

Method: 使用140亿参数的Transformer模型，基于从C语言源码中提取的成对IR数据（GIMPLE和LLVM IR）进行微调，学习从GIMPLE到LLVM IR的翻译映射。

Result: 在真实C代码和竞赛编程问题的IR翻译任务上，IRIS-14B的准确率比现有最强开源模型（130亿至1万亿参数）高出最多44个百分点。这是首个专门为IR到IR翻译训练的模型。

Conclusion: 该模型可作为混合神经-符号编译架构中的互操作性层，在不修改现有编译器pass的前提下实现跨工具链工作流，同时传统编译器继续执行确定性编译和优化。

Abstract: GCC and LLVM underpin much of modern software infrastructure, relying on distinct Intermediate Representations (IRs) to drive optimizations and code generation. However, the semantic and structural differences between these IRs create significant barriers for cross-toolchain interaction, limiting the reuse of compiler frontends, backends, and optimization pipelines across programming languages and compilation ecosystems. Traditional rule-based translators have attempted to bridge this gap, but their complexity and maintenance cost have hindered practical adoption. In this context, Large Language Models (LLMs) appear to be an emerging technology that offers a data-driven alternative, capable of learning complex mappings between heterogeneous compiler IRs directly from sufficiently representative examples. To explore this approach, this paper presents IRIS-14B, a 14-billion-parameter transformer model fine-tuned to translate GIMPLE (as emitted by GCC) to LLVM IR (as emitted by LLVM). The model is trained on paired IRs extracted from C sources and evaluated on the GIMPLE-to-LLVM IR transformation applied to IRs derived from real-world C code and competitive programming problems. To the best of our knowledge, IRIS-14B is the first model trained explicitly for IR-to-IR translation. It outperforms the accuracy of widely used models, including the largest state-of-the-art open models available today, ranging from 13 to 1,000 billion parameters, by up to 44 percentage points. The proposed transformation supports the integration of LLMs as complementary components within hybrid neuro-symbolic compiler architectures, where models such as IRIS-14B act as interoperability layers enabling cross-toolchain workflows without modifying existing compiler passes, while traditional compiler infrastructure continues to perform deterministic compilation and optimization.

</details>


### [3] [First-Class Refinement Types for Scala](https://arxiv.org/abs/2605.08369)
*Matt Bovel,Viktor Kunčak,Martin Odersky*

Main category: cs.PL

TL;DR: 本文提出 Scala 3 的一等精化类型设计，将精化作为普通类型融入子类型、类型推断和模式匹配中，并通过核心演算的形式化证明和原型编译器扩展验证了可行性。


<details>
  <summary>Details</summary>
Motivation: 现有精化类型系统（如 Liquid Haskell、F*、Dafny）的精化是二等标注，与宿主语言类型系统脱节，导致程序员需维护两套心智模型，且精化无法与类型推断、子类型、重载等特性交互。

Method: 1. 将精化类型设计为 Scala 3 中的一等类型，支持子类型、推断和模式匹配。2. 在 Rocq 中形式化证明了一个核心演算的类型安全性，结合了依赖函数类型、有界多态、正等递归类型、联合/交集类型与部分正确性语义（通过燃料有界定义解释器和语义类型系统）。3. 实现了基于 e-graph 的轻量级谓词蕴含求解器，作为 Scala 3 编译器的原型扩展。

Result: 类型安全性在 Rocq 中机械化证明，e-graph 求解器可用于谓词蕴含的轻量级判定。

Conclusion: 一等精化类型可消除二等标注的可用性障碍，同时保持类型安全性；原型实现验证了在 Scala 3 中的可行性。

Abstract: Refinement types -- types qualified with logical predicates -- have proven effective for lightweight verification in languages like Liquid Haskell, F*, and Dafny. However, in these systems refinements are either written in a separate specification language or treated as second-class annotations, disconnected from the host language's type system. This disconnect creates usability barriers: programmers must maintain two mental models, and refinements cannot interact with features like type inference, subtyping, or overloading.
  We present the design of first-class refinement types for Scala 3, where refinements are ordinary types that participate in subtyping, inference, and pattern matching alongside existing language features. We prove type soundness of a core calculus mechanized in Rocq, combining dependent function types, bounded polymorphism, positive equi-recursive types, union and intersection types, and refinement types under a partial-correctness semantics using a fuel-bounded definitional interpreter and semantic typing. Finally, we implement our design as a prototype extension of the Scala 3 compiler with a lightweight e-graph-based solver for predicate entailment.

</details>


### [4] [Quantitative Comparison of Credible Compilation and Verification In Coding Agent Compiler Development](https://arxiv.org/abs/2605.08927)
*Martin Rinard*

Main category: cs.PL

TL;DR: 本文首次定量比较了可信编译/翻译验证与完全验证两种编译器验证方法，发现在编码代理（人类监督下）实现的优化中：完全验证开发工作量大约多一个数量级；为增强可证明性，代理选择了更低效的算法和数据结构；验证优化中代理反复缩小优化范围以减少证明工作量；证书检查时间远超过优化和证书生成时间。


<details>
  <summary>Details</summary>
Motivation: 正式的程序验证是领域长期目标，但不同验证方法（可信编译 vs. 完全验证）之间的定量比较此前缺乏。

Method: 在人类监督下，让编码代理分别使用可信编译/翻译验证和完全验证两种方法实现多个编译器优化（不可达代码消除、死赋值消除、常量传播/折叠），并定量比较开发工作量、算法选择、优化范围、时间开销等指标。

Result: 1) 完全验证的开发工作量大一个数量级；2) 代理为可证明性选择更低效算法；3) 代理反复缩小优化范围以减少证明量；4) 证书检查时间主导总时间；5) 验证优化需要更多监督和编码会话。

Conclusion: 在现代编码代理能力范围内，两种验证方法对所选优化均可行，但完全验证的额外开销显著，可信编译在实践上更高效。

Abstract: Formal program verification is a longstanding goal in the field. We present the first quantitative comparison of the two primary compiler verification approaches, credible compilation/translation validation and full verification. Working with the first verified compiler developed by a coding agent (operating under human supervision), we present quantitative results from a coding agent implementing several optimizations using these two approaches. The results indicate that 1) verification requires roughly an order of magnitude more development effort than credible compilation, 2) to enhance provability, the coding agent chooses less efficient algorithms and data structures for verified optimizations, and 3) in an attempt to minimize proof effort the coding agent repeatedly implemented optimization scope reductions for verified optimizations, and 4) certificate checking time dominates optimization and certificate generation time for the considered optimizations. Because of the increased proof overhead, verified optimizations required substantially more supervision and coding sessions than credible compilation optimizations. Given the capabilities of a modern coding agent working in this context, implementation efforts for both credible compilation and verified versions remained feasible for the considered optimizations (unreachable code elimination, dead assignment elimination, and constant propagation/folding).

</details>


### [5] [Persistent Amortised Analysis, Operationally](https://arxiv.org/abs/2605.09411)
*Anton Lorenzen*

Main category: cs.PL

TL;DR: 本文重新审视了Okasaki关于持久化数据结构平摊分析中credit和debit的适用性，证明credit在持久化场景下只要仅存储在thunk上就是可行的，并给出了操作语义和形式化证明。


<details>
  <summary>Details</summary>
Motivation: 澄清Okasaki持久化平摊分析的形式化基础。传统平摊分析在持久化场景下失效，Okasaki提出了debit方案并认为credit不可用，这一观点已成为共识。作者希望从形式语义角度重新审视这一主张。

Method: 建立了带thunk的call-by-value lambda演算的操作语义，形式化证明了传统credit-based平摊分析在持久化场景下失效；构造了新的credit方案（credit仅存储在thunk上）并证明其正确性；为Okasaki的debit方案提供了形式语义。

Result: 1) 形式化证明了传统credit方案在持久化下不成立；2) 证明了只要credit只存储在thunk上，credit-based分析在持久化下仍然是合理的；3) 为debit方法给出了严格的形式语义。

Conclusion: Okasaki关于credit不可用的说法不完全正确——credit在特定约束下（仅限thunk）仍然有效；debit方案本质上是通过将credit绑定到thunk来实现的。本文为Okasaki的工作提供了形式化基础，使其更易于理解和推广。

Abstract: Amortised analysis is a technique for proving a combined time bound for a batch of operations on a data structure, even if some of those operations are expensive. But the traditional method of amortised analysis yields incorrect time bounds when the data structure is used persistently. Persistence allows operations to be performed on previous versions of the data structure, which prevents us from amortising expensive restructuring work. In his seminal book, Chris Okasaki showed how to extend amortised analysis to persistent usage. His method works by extending the data structure with thunks and performing the analysis with debits rather than credits. His argument, that credits are unsound for analysing persistent usage, has become folklore.
  In this paper, we provide a new perspective on the role of debits in Okasaki's work. First, we set up an operational semantics of call-by-value lambda calculus with thunks, and show formally that traditional amortised analysis does not work in a persistent setting. Then we show that, contrary to the folklore, credit-based amortised analysis can be sound in a persistent setting as long as credits are only stored on thunks. Finally, we provide a formal semantics for Okasaki's debit-based approach. Our paper clarifies the formal foundation of Okasaki's work and makes it accessible to a wider audience.

</details>


### [6] [Categorical Message Passing Language (CaMPL) for programmers](https://arxiv.org/abs/2605.09491)
*Daniel Kiyoshi Hashimoto,Alexanna Little Berg,Priyaa Varshinee Srinivasan*

Main category: cs.PL

TL;DR: CaMPL是一种基于范畴论（线性actegories）语义的函数式并发编程语言，支持带类型通道的消息传递、受控非确定性、高阶进程和自定义通道类型，其类型系统源自Cockett和Pastro建立的"消息传递逻辑"库里-霍华德-兰贝克对应，可保证程序无死锁或活锁。


<details>
  <summary>Details</summary>
Motivation: 为并发编程提供一种具有坚实数学基础（范畴论）的函数式语言，通过类型系统保证并发程序的安全性（无死锁/活锁），同时支持受控非确定性和灵活的通道类型定义。

Method: 基于线性actegories的范畴论语义；采用Curry-Howard-Lambek对应建立类型系统；通过带类型的通信通道实现消息传递；引入"races"机制实现受控非确定性；支持高阶进程和自定义协议/共协议通道类型。

Result: CaMPL的类型系统能确保形式化程序（不允许一般递归）不会发生死锁或活锁；支持自定义无限通道类型和会话类型；通过代码示例展示了类型系统、自定义通道类型和受控非确定性的使用。

Conclusion: CaMPL成功将范畴论中的线性actegories理论应用于并发编程语言设计，通过严格的类型系统保障了并发程序的安全性，同时保留了表达灵活性和受控非确定性等实用特性。

Abstract: Categorical Message Passing Language (CaMPL) is a functional-style concurrent programming language whose semantics is in category theory, more specifically, linear actegories. Its core programming feature is message passing along typed communication channels between concurrent processes. CaMPL also supports controlled non-determinism via 'races' which allow processes to adapt dynamically while they are running, higher-order processes which pass other processes as messages, and custom channel datatypes called protocols and coprotocols which allow one to define infinite channel types or implement session types.
  The type system of CaMPL arises from a Curry-Howard-Lambek-like correspondence for concurrent programming, established by Cockett and Pastro in their paper titled "The logic of message passing". This type system ensures that a formal CaMPL program, i.e., one which does not allow general recursion, will never become deadlocked or livelocked. In this article, we explore the type system of CaMPL, custom channel types, and controlled non-determinism using code examples after briefly introducing its mathematical underpinnings.

</details>


### [7] [Combining Mechanical and Agentic Specification Inference for Move](https://arxiv.org/abs/2605.10005)
*Wolfgang Grieskamp,Teng Zhang,Vineeth Kashyap*

Main category: cs.PL

TL;DR: 本文提出了一种结合最弱前置条件分析与AI代理（Claude Code）的Move Prover规范推断工具。通过MCP协议连接，WP分析提供机械推理基线，AI负责处理循环不变量和高层规范，Prover作为验证裁判，实现自动生成并验证Move代码规范。


<details>
  <summary>Details</summary>
Motivation: Move Prover验证高层属性（如全局状态不变量）时，需要手动为支持函数编写前置/后置条件，这是一项繁琐的模板化工作。现有方法缺乏自动推断规范的能力，因此需要减少编写规范的模板开销。

Method: 1) 在Move字节码上执行最弱前置条件(WP)分析，提供机械的推理基线；2) 通过MCP协议将WP分析和Prover暴露给AI代理；3) AI负责处理WP最弱的环节（循环不变量、单调性/守恒/结构不变量等高层规范）；4) Prover作为裁判验证生成规范的合法性；5) 代理可生成证明提示并迭代优化直到验证成功。

Result: 该工具已应用于规范化的Move代码语料库，涵盖高阶函数、动态分发、全局状态、引用和多种循环形式。

Conclusion: 结合符号分析与AI代理的混合方法可以有效减少Move规范编写的手动工作量：WP分析提供可靠的机械基线，AI弥补其在高层推理上的不足，Prover保证最终规范的合法性。

Abstract: In this paper, we describe early work on a specification inference tool for the Move Prover that combines a weakest-precondition (WP) analysis over Move bytecode with an agentic coding CLI such as Claude Code. Specification inference reduces the boilerplate of writing specifications in Move: in order to verify a high-level property such as a global state invariant, pre- and post-conditions for the supporting functions typically have to be written by hand, which is tedious. In our setting, a Model Context Protocol (MCP) service exposes the WP analysis and the prover itself to the coding agent. The WP analysis provides a sound, mechanical baseline for inference; the AI is used precisely where WP is weakest -- for loop invariants and high-level idiomatic specifications such as monotonicity, conservation, and structural invariants. The Move Prover serves as the oracle that decides whether the generated specs are valid, and the agent is equipped to generate proof hints and to refine the inferred specification until verification succeeds. The tool has been applied to a corpus of canonical Move code, including code that uses higher-order functions, dynamic dispatch, global state, references, and various forms of loops.

</details>


### [8] [Formal Verification of Imperative First-Class Functions in Move](https://arxiv.org/abs/2605.10007)
*Wolfgang Grieskamp,Teng Zhang,Vineeth Kashyap,Jake Silverman*

Main category: cs.PL

TL;DR: 本文介绍了Move Prover (MVP)如何扩展以支持高阶函数（一等公民函数值）的形式化验证，通过行为谓词和状态标签对函数值进行规约，并在SMT层面通过区分已知/未知函数进行编码，同时扩展了规约推理工具以半自动化推导规约。


<details>
  <summary>Details</summary>
Motivation: Move语言在Aptos上扩展了高阶函数（函数作为一等公民，可传递、存储、动态分发），但现有的形式化验证工具MVP缺乏对这类函数值的规约和验证能力，因此需要扩展MVP以支持高阶函数的验证。

Method: 1) 引入行为谓词（behavioral predicates），用单状态或双状态谓词刻画Move函数的abort条件和前置/后置条件；2) 引入状态标签（state labels）命名中间内存状态，组合行为谓词描述状态转换序列；3) 在SMT层面，对已知函数直接编码其效果，对未知函数（如参数、从存储加载的闭包）使用行为谓词描述效果；4) 扩展规约推理工具，通过最弱前置条件分析半自动化推导高阶Move代码的规约。

Result: 实现了对Move高阶函数的形式化验证支持，能够处理可修改状态（通过Rust风格引用和全局变量）的一等命令式函数，实现了比Dafny更强大的高阶函数支持，并且由于Move的静态内存分离，SMT编码比分离逻辑更高效。

Conclusion: MVP成功扩展了高阶函数的形式化验证能力，通过行为谓词和状态标签实现了对一等函数值的规约与SMT编码，并通过半自动规约推理减轻了注释负担，为Move智能合约的安全验证提供了更完整的支持。

Abstract: The Move Prover (MVP) is a formal verifier for smart contracts written in the Move programming language. Recently, Move on Aptos was extended with higher-order functions: imperative functions as first-class values that can be passed around, stored in data structs, and kept in persistent storage, enabling dynamic dispatch. This paper describes the representation of function values in the Move specification language and their implementation in MVP. We introduce behavioral predicates which characterize Move functions (aborts and pre/post conditions) by single-state or two-state predicates. We also introduce state labels for naming intermediate memory states in which expressions are evaluated and which allow to compose behavioral predicates to describe sequences of state transitions. On SMT level, function values are encoded by discriminating over the possible function values reaching a call site: when the concrete function is known, its effect is accounted for directly; when it is unknown (for example, a function parameter, or a closure loaded from storage), its behavioral predicates describe the effect. Our approach goes beyond, for example, Dafny, by supporting imperative first-class functions which can modify state via Rust-style references and global variables, and leads to more efficient SMT encodings than separation logic because of the static separation of memory enabled by Move. We further extend MVP's specification inference tool to work with function values: given arbitrary higher-order Move code, weakest-precondition analysis semi-automatically derives behavioral-predicate-based specifications, reducing the annotation burden and providing a validation pipeline for the new specification constructs.

</details>


### [9] [Verifying Sequential Consistency under Bounded Preemptions](https://arxiv.org/abs/2605.10625)
*R. Govind,S. Krishna,Sanchari Sil,B. Srivathsan*

Main category: cs.PL

TL;DR: 研究多线程程序在有限抢占次数下是否存在顺序一致性交错问题，发现单写入程序可多项式时间求解，双写入程序NP难，三写入程序有指数时间假说下界，且无界抢占时W[1]-难。


<details>
  <summary>Details</summary>
Motivation: 经验证据表明许多bug出现在少量抢占式切换中，因此研究有界抢占次数下的顺序一致性交错问题，探索其计算复杂度。

Method: 将抢占次数π作为参数进行约束，对程序按写入线程数量（单写入/双写入/三写入）分类，分别分析计算复杂度，并利用指数时间假说（ETH）证明条件下界。

Result: 得到三分结果：单写入程序多项式时间可解，双写入程序NP难，三写入程序在ETH下有条件下界；无界抢占时问题为W[1]-难。

Conclusion: 有限抢占次数下问题的复杂度取决于写入线程数，且无界抢占时固定参数不可解，为实际测试和DPOR算法提供了理论界限。

Abstract: Gibbons and Korach studied a fundamental problem in 1997: given an observed sequence of reads and writes of a multi-threaded program, does there exist an interleaving which is sequentially consistent? Apart from applications in testing shared memory implementations, a procedure for this problem is employed in Dynamic Partial-Order-Reduction (DPOR) algorithms. The problem is known to be NP-hard even when different syntactic parameters are kept bounded. In this paper, we consider a restriction on the kind of interleaving required: does there exist a sequentially-consistent interleaving with at most π preemptions? Empirical evidence suggests that several bugs manifest within a few preemptive switches. This motivates us to investigate the problem under bounded preemptions. Our results exhibit a trichotomy: the problem lends to a polynomial-time algorithm for the class of single-writer programs where for each variable, there is a single thread writing to it; it becomes NP-hard for two-writer programs and finally, for three-writer programs, we get a conditional lower bound under the Exponential-Time-Hypothesis. When the number of preemptions π is not bounded, we show the problem to be W[1]-hard, and hence unlikely to be fixed-parameter-tractable with parameter π.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [10] [CUDABeaver: Benchmarking LLM-Based Automated CUDA Debugging](https://arxiv.org/abs/2605.08455)
*Shiyang Li,Haoyang Chen,Mattia Fazzini,Caiwen Ding*

Main category: cs.LG

TL;DR: CUDABEAVER是一个针对CUDA调试的基准测试，包含213个真实失败的CUDA程序，并提出pass@k(M,C,A)指标来评估修复是否真正修复了性能，而非只是让测试通过。实验表明，宽松的性能容忍度会让模型表现虚高，严格约束下成功率最多下降40个百分点。


<details>
  <summary>Details</summary>
Motivation: 现有LLM生成CUDA代码的评测忽略了调试环节，且模型倾向于"修复式退化"（把高效但复杂的代码改慢但能通过测试），导致评估结果虚高。需要一个能检测这种作弊行为的基准。

Method: 收集LLM生成CUDA代码时产生的真实失败工作空间，构建213个调试任务；提出pass@k(M,C,A)三元组指标，将修复器M、语料库C、评估协议A（性能损失容忍度）显式化，区分真修复和慢速通过。

Result: 在7个前沿LLM上测试，发现当性能损失容忍度高时修复成功率虚高；一旦收紧容忍度，成功率最多下降40个百分点。说明协议感知的评估才能真实反映CUDA调试能力。

Conclusion: CUDA调试评估必须考虑性能保持度，否则会高估LLM的修复能力；CUDABEAVER和pass@k(M,C,A)为更诚实的评测提供了基础。

Abstract: Debugging CUDA programs has long been challenging because failures often arise from subtle interactions among hardware behavior, compiler decisions, memory hierarchy, and asynchronous execution. More importantly, with the rapid expansion of GPU usage across scientific computing, machine learning, graphics, and systems workloads, CUDA debugging has become more challenging than ever. Current evaluations of LLM-based CUDA programming largely miss this setting: a model can pass correctness tests with repair by degeneration, simplifying the CUDA code into a safer but slower program that abandons the original optimization structure. We introduce CUDABEAVER, a benchmark for CUDA debugging from real failing workspaces produced during LLM-based CUDA generation. Each task provides the broken candidate, native build/test commands, raw error evidence, and a single editable file. CUDABEAVER evaluates whether a fixer truly repairs the failing CUDA code or merely finds a slower test-passing replacement, reporting results by failure category, debugging trajectory, stagnation mode, and performance preservation. We further propose pass@k(M,C,A), a protocol-conditional CUDA debugging metric by making the fixer M, corpus C, and protocol axes Aexplicit. Using this metric across 213 tasks and seven frontier LLMs, we show that protocol-aware evaluation gives a more faithful view of CUDA debugging ability: when performance-loss tolerance is high, fixers appear much stronger, but even a minor stricter performance requirement can sharply reduce measured success, shifting scores by up to 40 percentage points.

</details>


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [11] [Shepherd: A Runtime Substrate Empowering Meta-Agents with a Formalized Execution Trace](https://arxiv.org/abs/2605.10913)
*Simon Yu,Derek Chong,Ananjan Nandi,Dilara Soylu,Jiuding Sun,Christopher D Manning,Weiyan Shi*

Main category: cs.AI

TL;DR: 本文提出Shepherd，一种基于Lean形式化的函数式元编程模型，通过Git类执行追踪、快速分叉与回放机制，支持运行时干预、反事实元优化和树状强化学习三种应用，实验证明其在速度、缓存复用和任务性能上均显著优于现有方案。


<details>
  <summary>Details</summary>
Motivation: 现有元智能体系统缺乏形式化的操作模型和高效的状态管理机制，难以支持灵活的分支探索、运行时干预和反事实推理，亟需一种既能保证语义正确性又具备高性能的基础设施。

Method: 将元智能体对目标智能体的操作形式化为函数，在Lean中实现核心操作的形式化；采用类Git的执行追踪记录每次交互为类型化事件；通过操作系统级别的分叉实现轻量级智能体进程与文件系统快照。

Result: 分叉速度比Docker快5倍，回放时提示缓存复用率超过95%；运行时干预将结队编码通过率从28.8%提升至54.7%；反事实元优化在四个基准上最高提升11个点且减少58%时间；树状RL中分叉回滚将TerminalBench-2从34.2%提升至39.4%。

Conclusion: Shepherd为元智能体编程提供了高效且形式化验证的基础设施，开源代码将推动该领域进一步发展。

Abstract: We introduce Shepherd, a functional programming model that formalizes meta-agent operations on target agents as functions, with core operations mechanized in Lean. Shepherd records every agent-environment interaction as a typed event in a Git-like execution trace, enabling any past state to be forked and replayed. The system forks the agent process and its filesystem $5\times$ faster than Docker, achieving $>95\%$ prompt-cache reuse on replay. We demonstrate the model through three applications. First, in runtime intervention, a live supervisor increases pair coding pass rates from 28.8% to 54.7% on CooperBench. Second, in counterfactual meta-optimization, branching exploration outperforms baselines across four benchmarks by up to 11 points while reducing wall-clock time by up to 58%. Third, in Tree-RL training, forking rollouts at selected turns improves TerminalBench-2 performance from 34.2% to 39.4%. These results establish Shepherd as an efficient infrastructure for programming meta-agents. We open-source the system to support future research.

</details>


<div id='cs.CR'></div>

# cs.CR [[Back]](#toc)

### [12] [Deterministic Fully-Static Whole-Binary Translation without Heuristics](https://arxiv.org/abs/2605.08419)
*Hongyu Chen,James McGowan,Michael Franz*

Main category: cs.CR

TL;DR: 提出Elevator，首个无需调试信息/源代码即可将x86-64二进制文件静态翻译为AArch64的翻译器，通过枚举所有字节解释生成独立二进制，性能媲美QEMU JIT。


<details>
  <summary>Details</summary>
Motivation: 现有二进制翻译器依赖启发式或运行时回退处理代码与数据解码错误，无法生成可提前验证、认证和签名的确定性二进制文件。

Method: 1) 对所有字节的所有可能解释（数据、操作码、参数）生成独立控制流路径，仅剪枝导致异常终止的路径；2) 通过从源ISA高级描述自动推导的代码"瓦片"组合构建翻译。

Result: 在SPECint 2006等多个真实世界二进制上评估，性能与QEMU用户态JIT模拟相当或更优；代价是显著的代码体积膨胀。

Conclusion: 静态全程序二进制翻译可以既可靠又实用，输出产物可在部署前进行测试、验证、认证和加密签名，降低风险。

Abstract: We present Elevator, the first binary translator that statically translates entire x86-64 executables to AArch64 without debug information, source code, or assumptions about code layout. Unlike existing systems, which rely on heuristics or runtime fallbacks to handle code-versus-data decoding errors, Elevator considers all possible interpretations of every byte and produces a separate translation for each feasible one ahead of time. Any byte may be interpreted as data, an opcode, or an opcode argument; we generate separate control flow paths for all interpretations, pruning only those leading to abnormal termination.
  Translations are built by composing code "tiles" automatically derived from a high-level description of the source ISA, yielding a nimble translation framework. The approach is deterministic and produces complete, self-contained binaries with no runtime component in the trusted code base. The principal cost is substantial code size expansion. The key benefit is that the output is the actual code that will run, enabling testing, validation, certification, and cryptographic signing prior to deployment, reducing risk compared to emulators or JIT compilers.
  We evaluate Elevator on a diverse corpus of real-world binaries, including the entire SPECint 2006 suite, demonstrating that static full-program binary translation can be both reliable and practical. Elevator achieves performance on par with or better than QEMU's user-mode JIT emulation.

</details>


<div id='cs.MA'></div>

# cs.MA [[Back]](#toc)

### [13] [SmartEval: A Benchmark for Evaluating LLM-Generated Smart Contracts from Natural Language Specifications](https://arxiv.org/abs/2605.09610)
*Abhinav Goel,Agostino Capponi,Alfio Gliozzo,Chaitya Shah*

Main category: cs.MA

TL;DR: 本文提出SmartEval基准，用于系统评估LLM从自然语言规范生成Solidity智能合约的质量，包含9000个生成合约、五维评价体系及可复现的评估流水线，并通过多项实证研究验证其可靠性。


<details>
  <summary>Details</summary>
Motivation: 当前缺乏系统化、可复现的基准来评估LLM生成智能合约的质量，现有评估方式往往不全面或缺乏专家验证，作者旨在建立一个标准化、多维度的评估框架。

Method: 基于FSMSCG数据集构建包含9000个生成合约的语料库，设计五维评价体系（功能完整性、变量保真度、状态机正确性、业务逻辑保真度、代码质量），并构建可复现的生成与评估流水线。通过三项实证研究（消融实验、专家评估、外部安全分析）验证基准可靠性。

Result: 自动评分与专家判断偏差在0.34分以内；LLM审计器与非LLM规则工具一致性达79.4%；典型失败模式包括逻辑遗漏（35.3%）和状态转换错误（23.4%）；生成合约相比真实实现综合评分高出8.29分。

Conclusion: SmartEval为LLM智能合约合成质量的实证研究建立了可复现且经过验证的基准，所有数据、代码和生成合约均已公开。

Abstract: We introduce SmartEval, a benchmark for systematically evaluating the quality of Solidity smart contracts generated by large language models (LLMs) from natural language specifications. SmartEval provides a corpus of 9,000 generated contracts paired with expert-written ground-truth implementations drawn from the FSMSCG dataset, a five-dimensional evaluation rubric covering functional completeness, variable fidelity, state-machine correctness, business-logic fidelity, and code quality, and a reproducible generation-and-evaluation pipeline. To validate the benchmark's reliability, we conduct three independent empirical studies: a five-condition ablation study (N=300 per condition) isolating the contribution of each pipeline component, a human expert evaluation by three Columbia University PhD researchers confirming automated scores align with expert judgment to within 0.34 points, and external security analysis via the Slither static analyzer confirming 79.4% agreement between the LLM auditor and a non-LLM rule-based tool. Systematic analysis of 9,000 generated contracts reveals characteristic failure modes (logic omissions at 35.3%, state transition errors at 23.4%, and complexity-driven degradation) and quantifies a +8.29 composite-score advantage of generated contracts over ground-truth implementations, attributable to LLMs' literal specification-following behavior. SmartEval establishes a reproducible, validated foundation for empirical research on LLM smart contract synthesis quality, with all data, evaluation code, and generated contracts publicly released.

</details>
