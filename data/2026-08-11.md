<div id=toc></div>

# Table of Contents

- [cs.PL](#cs.PL) [Total: 9]
- [cs.CR](#cs.CR) [Total: 2]
- [cs.FL](#cs.FL) [Total: 1]
- [stat.ML](#stat.ML) [Total: 1]
- [cs.CV](#cs.CV) [Total: 1]
- [cs.AI](#cs.AI) [Total: 1]
- [cs.LG](#cs.LG) [Total: 1]
- [cs.CY](#cs.CY) [Total: 1]
- [cs.OS](#cs.OS) [Total: 1]


<div id='cs.PL'></div>

# cs.PL [[Back]](#toc)

### [1] [Reduce Once, Verify Many: Verifying Isolation Guarantees via Hierarchical Abstractions](https://arxiv.org/abs/2608.07793)
*Shabnam Ghasemirad,Christoph Sprenger,Si Liu,David Basin*

Main category: cs.PL

TL;DR: 本文提出了一种数学严谨、系统化的方法来验证数据库隔离保证，支持七种隔离级别，揭示强隔离级别可通过精化(refinement)验证而弱隔离级别还需归约(reduction)的基本二分法，并通过抽象模型层级大幅简化证明，所有结果在Isabelle/HOL中正式化。


<details>
  <summary>Details</summary>
Motivation: 数据库隔离级别的验证在数学上具有挑战性，现有方法缺乏系统性和严谨性，且证明工作量大、重复性高。作者希望提出一种系统化方法，使验证更简单、更统一，并减少用户证明负担。

Method: 提出一个分层的抽象模型体系：(1)支持七种隔离级别；(2)识别出强隔离级别与弱隔离级别之间的根本二分法——强级别仅需精化验证，弱级别还需归约；(3)在高层抽象中一次性完成归约，消除每个协议单独进行归约证明的需求；(4)开发了通用的归约理论，并在Isabelle/HOL中形式化所有结果。

Result: 将所有用户的证明工作简化为对层级中最具体模型的单次、更简单的精化证明，并通过通用的高层归约理论消除了弱隔离级别下逐协议归约证明的负担。所有成果已在Isabelle/HOL中完成机器验证。

Conclusion: 该方法显著降低了数据库隔离验证的门槛和复杂度，该归约理论本身也具有更广泛的通用价值，为形式化验证数据库隔离保证提供了高效的框架。

Abstract: We present a mathematically rigorous, systematic approach for the verification of database isolation guarantees, which (i) supports a spectrum of seven isolation levels, (ii) uncovers a fundamental dichotomy among isolation levels: stronger levels can be verified via refinement alone, whereas weaker levels additionally require reduction, and (iii) provides a hierarchy of abstract models that substantially simplifies proofs by factoring out their most labor-intensive parts. In particular, we eliminate the need for per-protocol reduction proofs for the weaker class of isolation levels by performing a once-and-for-all reduction at a high level of abstraction in our hierarchy. To achieve this, we develop and apply a generic theory of reduction, which is also of more general interest. Overall, our approach minimizes the user's proof effort to a single, simpler refinement of the most concrete model in our hierarchy. All our results are formalized in Isabelle/HOL.

</details>


### [2] [Refined^2 Environment Classifiers](https://arxiv.org/abs/2608.07888)
*Yuito Murase,Atsushi Igarashi*

Main category: cs.PL

TL;DR: 本文提出了一种用于MetaML风格多阶段编程（MSP）与可变状态交互的类型系统，通过改进的环境分类器（RECs）静态排除有害的作用域逃逸（scope extrusion），同时支持多级代码生成、运行时执行和跨阶段持久化（CSP）。系统细化分类器以同时跟踪变量作用域和分类器自身的作用域，并集成了分类器上的多态性，最终通过定义性解释器定义了操作语义，并在Rocq中完成了机械化证明。


<details>
  <summary>Details</summary>
Motivation: MetaML风格的多阶段编程支持准引用代码生成和运行时执行，但与计算效应（尤其是可变状态）交互时会产生微妙问题：可变状态可能导致作用域逃逸，即生成的代码逃逸出其所依赖变量的作用域。这需要静态类型系统来排除这种有害情况。

Method: 基于改进的环境分类器（RECs）构建类型系统，将分类器细化以同时跟踪变量作用域和分类器自身的作用域；集成分类器上的多态性以支持更通用和可复用的多级代码生成模式；通过定义性解释器定义操作语义，并利用Rocq进行机械化证明。

Result: 证明了类型健全性和离线代码生成的安全性，表明生成的代码可以作为独立的良类型程序提取。提供了在Rocq中的工作实现和机械化证明。

Conclusion: 该类型系统成功将RECs扩展到MetaML风格设置中，为解决MSP与可变状态交互中的作用域逃逸问题提供了静态保证，同时保持了多级代码生成的通用性和可复用性。

Abstract: MetaML-style multi-stage programming (MSP) supports quasi-quotation-based code generation, runtime execution of generated code, and cross-stage persistence (CSP). However, its interaction with computational effects is subtle: mutable state can cause scope extrusion, where generated code escapes the scope of variables on which it depends.
  This paper presents a type system for MetaML-style MSP with mutable state that statically rules out harmful scope extrusion while supporting multi-level code generation, runtime execution, and a variant of CSP. Our system builds on refined environment classifiers (RECs), a discipline that annotates code types with the variable scopes on which generated code depends. To scale RECs to the MetaML-style setting, we refine classifiers so that they track not only variable scopes, but also the scopes of classifiers themselves. Further, we integrated polymorphism over classifiers, enabling more general and reusable code generation patterns in a multi-level setting.
  For the resulting system, we define an operational semantics via a definitional interpreter and prove type soundness and safety of offline code generation, showing that generated code can be extracted as standalone well-typed programs. We provide working implementations and mechanized proofs in Rocq.

</details>


### [3] [ReOC: Compilation of Recursive Quantum Oracles with Recursion-Aware Uncomputation](https://arxiv.org/abs/2608.07973)
*Huiling Wu,Yuxin Deng*

Main category: cs.PL

TL;DR: 本文提出ReOC编译框架，将带量子控制流的高层递归量子预言机规范（RQIMP）编译为可逆量子程序（RQC++），通过索引静态寄存器管理和递归感知的反计算策略，在控制量子存储开销的同时避免指数级时间膨胀，并提供了编译正确性的数学证明。


<details>
  <summary>Details</summary>
Motivation: 量子预言机是许多量子算法的核心组件，其规范可能包含依赖运行时量子数据的递归控制流。然而现有可逆编译框架对这类量子控制的递归结构支持有限，直接编写RQC++代码繁琐且易出错。

Method: ReOC包含两个核心策略：(1) 索引静态寄存器纪律，在动态量子控制下隔离各递归层的活跃变量，实现安全的寄存器复用以控制量子存储；(2) 递归感知的反计算策略，对递归调用的临时变量采用延迟清理以控制时间开销，对非递归语句的临时变量采用急切清理以减少空间占用。

Result: 对于线性递归，该策略实现了与递归深度线性相关的时间开销，开销参数由每层寄存器占用和基本操作代价决定；同时提供了从RQIMP到RQC++编译正确性的数学证明，包括语义保持和临时量子变量的正确反计算。

Conclusion: ReOC成功填补了量子控制递归预言机编译的空白，在空间与时间开销之间提供了可调平衡，并具备严格的形式化正确性保证。

Abstract: Quantum oracles are essential to many quantum algorithms, and their specifications may involve recursive control flow that depends on runtime quantum data. However, existing reversible compilation frameworks provide limited support for such quantum-controlled recursive structures.
  We present ReOC, a compilation framework that transforms high-level recursive oracle specifications with quantum control flow into reversible quantum programs. The framework comprises RQIMP, a high-level imperative source language for specifying recursive oracles, and a method of compiling programs in that language into RQC++, an existing high-level quantum recursive language with quantum control flow. In this way, we avoid the tedious and error-prone process of directly writing quantum oracles in RQC++.
  To manage static storage under dynamic quantum control, ReOC uses an indexed static-register discipline to isolate live variables across recursion layers, enabling safe register reuse while controlling quantum storage usage. Furthermore, to address the exponential time blow-up caused by naive uncomputation in recursive settings, ReOC employs a recursion-aware uncomputation strategy: temporary variables from recursive calls are cleaned using deferred strategies to control time overhead, while those from non-recursive statements are cleaned eagerly to reduce space usage. For linear recursion, this strategy yields overhead linear in recursion depth, parameterized by the per-layer register footprint and primitive-operation costs. Finally, we provide a mathematical proof of compilation correctness from RQIMP to RQC++, establishing semantic preservation and correct uncomputation of temporary quantum variables.

</details>


### [4] [SimP: Unifying Syntax- and Semantic-Guided Techniques for Efficient Program Reduction](https://arxiv.org/abs/2608.08352)
*Ye Xiong,Xiangyu Gao,Qiaochu Chen,Mingyu Li,Haibo Chen*

Main category: cs.PL

TL;DR: SimP是一个结合传统规则约简与LLM引导的程序约简框架，通过定制提示设计和两阶段协同（规则约简+LLM约简），在保持约简质量的同时显著提升约简效率，且LLM成本可忽略不计。


<details>
  <summary>Details</summary>
Motivation: 编译器bug的触发程序往往过大，不利于实际调试。现有程序约简方法虽然约简质量好，但基于语法指导的规则删除策略采用试错式迭代删除，导致约简速度慢。

Method: SimP将传统约简与LLM引导的语法和语义约简相结合。通过定制化提示设计引导约简过程，并协同组合基于规则的约简阶段和基于LLM的约简阶段，以优化约简性能。

Result: 实验结果表明SimP在提升约简效率的同时实现了与现有方法相当的约简质量，且LLM货币成本可忽略不计。

Conclusion: SimP证明了LLM引导的程序约简在效率和成本方面具有可行性，为程序约简领域提供了一种新的高效方案。

Abstract: Compiler bugs are pervasive in modern compiler systems, but the test programs that trigger them are often too large for practical debugging. Program reduction addresses this by minimizing test program size while preserving the original bug-triggering behavior. Existing approaches mainly rely on syntax-guided, rule-based deletion strategies that iteratively remove parts of the program in a trial-and-error manner. While effective in reduction quality, these approaches suffer from slow reduction speed.
  This paper presents SimP, a program reduction framework that combines traditional reduction with LLM-based syntax- and semantic-guided reduction. SimP leverages customized prompt design to guide the reduction process. SimP synergistically combines rule-based and LLM-based reduction stages to optimize the reduction performance. The results show that SimP improves reduction efficiency while achieving comparable reduction quality, with negligible LLM monetary cost.

</details>


### [5] [Bona: Automatic Management of Dirty Ancilla Borrowing in Quantum Circuits](https://arxiv.org/abs/2608.08765)
*Xiaoquan Xu,Chenke Liu,Boning Meng,Zihao Shen,Li Zhou*

Main category: cs.PL

TL;DR: 本文首次形式化了脏量子比特借用问题，证明了其NP困难性，并提出了第一个面向脏量子比特借用的调度器BONA，采用深度感知启发式算法，在多种基准测试中平均减少约99%的脏辅助比特，且在并行量子行走场景中以更小的电路深度达到了与清洁量子比特方案相同的电路宽度。


<details>
  <summary>Details</summary>
Motivation: 辅助量子比特的管理是降低量子电路宽度的关键技术，脏辅助比特（可以从任意空闲量子比特借用而不论其初始状态）在宽度优化上具有很大灵活性，但此前只能依靠人工处理，容易出错且缺乏系统性支持。因此需要形式化该问题并提供自动化优化工具。

Method: 本文首先形式化定义了脏量子比特借用问题，并证明了该问题的NP困难性；随后提出BONA调度器，基于一种新颖的深度感知启发式算法来实现脏量子比特的自动借用调度。

Result: 在多种基准测试（包括实际量子电路和随机排列的真实电路模块组合）中，BONA平均减少了约99%的脏辅助比特，同时控制了深度开销。在并行量子行走场景中，BONA实现了与清洁量子比特方案（jiang2024recycling和quantinuum）相同的电路宽度，但电路深度显著更小。

Conclusion: 实验结果实证表明，脏量子比特借用在一定并行性的电路中具有独特的优化优势，为量子电路宽度优化提供了新的可能，BONA作为首个自动化调度器为该方向的工程实践奠定了基础。

Abstract: The management of ancilla qubits has become a critical technique for reducing quantum circuit width. Dirty ancillas, which may be borrowed from any temporarily idle qubit regardless of their initial states, offer substantial flexibility for width optimization, but their use has so far required manual and error-prone handling. We formalize the dirty-qubit borrowing problem and establish a fundamental computational limit by proving its NP-hardness. To support practical optimization, we present \bona, the first scheduler for dirty-qubit borrowing, built on a novel depth-aware heuristic algorithm. We evaluate \bona~ across a variety of benchmarks, including practical quantum circuits and randomly arranged compositions of real circuit modules, and find that it reduces nearly 99\% of dirty ancillas on average with controlled depth overhead. In particular, for parallel quantum walk---an essential component of parallel Hamiltonian simulation---\bona~ matches the circuit width achieved by the clean-qubit schemes of \citeauthor{jiang2024recycling}~(\citeyear{jiang2024recycling}) and \citeauthor{quantinuum}~(\citeyear{quantinuum}), but attains significantly smaller circuit depth, providing concrete evidence that dirty ancillas offer unique optimization advantages in circuits with certain parallelism.

</details>


### [6] [Sound Enforcement of Dynamic Release Information Flow Policy-Full Version](https://arxiv.org/abs/2608.09506)
*Jeffrey C. Ching,Danfeng Zhang*

Main category: cs.PL

TL;DR: 本文提出了首个能够强制实施动态释放（dynamic release）策略的类型系统，并形式化证明了其可靠性。该系统扩展了传统信息流分析，允许安全级别在运行时任意升降级，弥补了静态策略与现实安全需求之间的鸿沟。作者在Rust语言上实现了原型，并通过会议评审系统和Civitas案例研究验证了其有效性。


<details>
  <summary>Details</summary>
Motivation: 信息流分析是评估机密性和完整性的标准方法，但在实际系统中的应用仍不广泛。根本原因在于理论与实践的鸿沟：现实系统的安全需求是动态的（数据机密性会变化），而现有技术假设静态策略。虽然已有研究探索了去分类（declassification）、认可（endorsement）和调用策略，最近的工作提出了统一的动态释放策略，但如何可靠地（soundly）实施该策略仍然是一个未解决的问题。

Method: 本文提出了第一个用于实施动态释放策略的类型系统。具体包括：（1）形式化了一个支持动态释放策略的核心语言；（2）开发了一个检查动态释放策略的类型系统；（3）开发了新的证明技术并形式化证明了类型系统能够强制实施动态释放策略；（4）将类型系统作为Rust语言的扩展实现原型，并进行了会议评审系统和Civitas的案例研究。

Result: 作者成功开发并形式化验证了首个强制实施动态释放策略的类型系统，证明了其可靠性（soundness）。原型实现基于Rust语言，并通过两个现实系统（会议评审系统和Civitas）的案例研究验证了实用性。

Conclusion: 本文填补了动态释放策略在可靠实施方面的空白，通过类型系统弥合了信息流分析理论与现实安全需求之间的鸿沟，为动态安全策略的端到端实施提供了坚实的基础。

Abstract: Information flow analysis is the de facto method of assessing confidentiality and integrity issues. However, the widespread adoption of information flow analysis in real-world systems is still lacking, partly due to a fundamental gap between theory and practice: the dynamic nature of security concerns in real-world systems goes beyond the scope of existing techniques that assume a static policy (i.e., data secrecy does not change).
  Recognizing the fundamental gap, a substantial amount of research has studied various aspects of it (e.g., enabling declassification, endorsement, and invocation policies). A recent work takes a step further by formalizing a promising end-to-end policy called dynamic release that unifies prior formalizations by allowing information flow restrictions to downgrade and upgrade in arbitrary ways. However, how to soundly enforce the powerful dynamic release policy is still an open question.
  In this paper, we present the first type system that enforces dynamic release policy and formally prove its soundness. More specifically, we (1) formalize a core language that enables dynamic release policy, (2) develop a type system that checks dynamic release policy, (3) develop new proof techniques and formally prove that the type system enforces dynamic release policy, and (4) implement a prototype of the type system as an extension to the Rust language, along with case studies on conference reviewing system and Civitas.

</details>


### [7] [Quantum Uncomputation of Clean and Dirty Ancilla Qubits](https://arxiv.org/abs/2608.09578)
*Chenke Liu,Li Zhou,Boning Meng*

Main category: cs.PL

TL;DR: 本文首次提出同时支持干净和脏辅助量子比特（ancilla qubits）的自动反计算（uncomputation）统一形式化框架，证明检查反计算存在性是coNP-hard问题，并提出两种互补方法：基于重写的归一化算法（RwUn）和基于模板的推理系统（TpUn），在覆盖率上全面超越现有方法Reqomp。


<details>
  <summary>Details</summary>
Motivation: 现有的自动反计算研究仅关注干净辅助量子比特，尚未探索脏辅助量子比特（dirty ancillas）的场景。为了更广泛地支持量子计算中的辅助量子比特安全使用，需要统一的形式化框架来同时处理干净和脏辅助量子比特的反计算问题。

Method: 作者首先统一形式化定义了干净和脏辅助量子比特的反计算问题，并证明了检查反计算存在性是coNP-hard的。随后提出两种互补的存在性检查方法：(1) RwUn——基于重写的归一化算法；(2) TpUn——基于模板的推理系统，通过结构化的Store-Use模式保证反计算。两种方法均在Qiskit和Python中实现原型。

Result: 与现有最先进方法Reqomp相比，RwUn在实用复杂依赖基准上实现100%覆盖率，在随机经典电路上覆盖率提升两倍，在随机量子电路上实现约50%覆盖率，超出现有方法的适用范围，展示了更广泛的适用性。

Conclusion: 本文填补了脏辅助量子比特反计算研究的空白，通过统一形式化和两种互补的合成导向方法，显著扩展了自动反计算技术的适用范围和覆盖率，为该领域的进一步研究奠定了理论基础。

Abstract: Automatic uncomputation aims to provide programming-language-level support to facilitate the correct and safe use of ancilla qubits in quantum computing, but efforts have only been made for clean ancillas, leaving dirty ancillas unexplored. We present a unified formalization of the uncomputation of both clean and dirty ancillas. For the first time, we prove that checking the existence of uncomputation is coNP-hard. We introduce two complementary synthesis-oriented existence-checking methods: a rewrite-based normalization algorithm (RwUn) and a template-based reasoning system (TpUn) that guarantees uncomputation through structured Store-Use patterns. We implement prototypes of both methods in Qiskit and Python. Compared to the state-of-the-art Reqomp~\cite{reqomp}, RwUn achieves 100% coverage on practical complex-dependency benchmarks, twice the coverage on random classical circuits, and about 50% coverage on random quantum circuits beyond the scope of existing methods, demonstrating broader applicability.

</details>


### [8] [Categorical Models of Amortized Cost: An Adjoint Relationship between Cost and Potential](https://arxiv.org/abs/2608.09635)
*David Binder,David Corfield,Dominic Orchard,Vineet Rajani*

Main category: cs.PL

TL;DR: 本文识别了用于跟踪计算代价和势能（cost and potential）的类型理论的 denotational 模型所需满足的抽象性质：代价和势能必须由一对伴随的分级函子（adjoint pair of graded functors）建模，其中建模代价的函子既构成分级 monad 又构成兼容的分级 comonad。作者给出了该抽象方案的三个具体实例。


<details>
  <summary>Details</summary>
Motivation: 现有的代价跟踪类型系统（如 λ-amor）能够同时跟踪最坏情况代价和摊还代价（势能），但缺乏对这类系统的 denotational 模型的系统化抽象理解。作者希望提取出这些模型必须满足的抽象范畴论性质，从而更广泛地理解和构建此类模型。

Method: 作者识别出核心抽象结构：代价和势能应由一对伴随的分级函子建模，其中代价函子既是分级 monad 又是兼容的分级 comonad。然后基于这一抽象方案给出三个具体实例：(1) 忽略代价的简单集合论模型；(2) 原始 λ-amor 论文中的 Kripke logical relations 模型（证明可转化为伴随模型的实例）；(3) 基于 monoidal 范畴上 copresheaf 的新模型，其中对和函数分别由 Day convolution 及其右伴随建模。

Result: 本文成功识别了代价和势能类型理论 denotational 模型的抽象范畴论性质（伴随分级函子对），并将三个不同的具体模型统一到这个抽象框架下，包括一个新提出的基于 copresheaf 和 Day convolution 的模型。

Conclusion: 该工作需要由伴随分级函子对的统一框架，为代价和势能跟踪类型系统的 denotational 语义提供了统一的抽象理解，并为构建新的模型提供了指导原则。

Abstract: Various type systems have been developed to track the cost $κ$ of a computation using a cost-tracking monad $M\ κτ$. On its own, this only tracks the worst-case cost of a computation. If we also want to track amortized cost, then we can add a type $[κ]τ$ which stores potential $κ$ with a type $τ$, together with operations for storing and releasing potential. In this work, we build on one such system, $λ$-amor: $λ$-amor allows to track cost and potential in the type system and subsumes effect and coeffect-based systems, call-by-value and call-by-name based languages. In this paper, we identify the abstract properties that denotational models of type theories for cost and potential have to satisfy: Cost and potential must be modelled by an adjoint pair of graded functors, where the functor modelling cost forms both a graded monad and a compatible graded comonad. We present three concrete instances of this general abstract scheme: (1) A simple set-theoretic model that ignores the cost tracked by the type system, (2) the Kripke logical relations model in the original $λ$-amor paper (which we show can be turned into an instance of the adjoint model), and (3) a novel model based on copresheaves on a monoidal category of costs, where we model pairs and functions by Day convolution and its right-adjoint.

</details>


### [9] [Abstract Compilation as Abstraction of Operator Semantics, applied to Cost Analysis](https://arxiv.org/abs/2608.09769)
*Louis Rustenholz,Alessio Mansutti,Pedro López-García,Félix Ridoux,Niki Vazou,Manuel V. Hermenegildo*

Main category: cs.PL

TL;DR: 本文提出算子语义（operator semantics），作为介于语法与经典指称语义之间的语义中间表示，将程序视为算子；抽象编译被理解为对这类算子进行抽象。作者开发了针对函数、算子和程序本身的高阶抽象域，以复合（composition）为核心新原语，并构建了用于构造可靠、精确且模块化抽象编译器的范畴框架。该框架在基于递归的静态代价分析中实例化，提出了求解器无关、最优的递归提取技术，支持一般函数未知量和余代数（catamorphic）度量。


<details>
  <summary>Details</summary>
Motivation: 最小不动点在程序语义中至关重要，但它们抽象掉了生成递归的结构信息。现有指称语义无法保留程序的递归结构，导致在进行如代价分析等需要结构信息的静态分析时信息丢失或不够精确。

Method: 引入算子语义作为语法与经典指称语义之间的中间表示；构建函数、算子和程序的高阶抽象域，以复合为关键新原语；提出范畴框架用于构造可靠、精确、模块化的抽象编译器；在递归代价分析中实例化，发展求解器无关的最优递归提取技术，支持一般函数未知量和余代数度量。

Result: 在基于递归的静态代价分析背景下成功实例化该框架，实现了求解器无关的最优递归提取，并支持比传统方法更广泛的度量类别（余代数度量）和一般函数未知量。

Conclusion: 算子语义为程序语义提供了一种新的中间层次，通过抽象编译与高阶抽象域的结合，能够在保留递归结构信息的同时实现可靠、精确且模块化的程序分析，尤其在递归代价分析方面展现出超越传统方法的表达能力。

Abstract: Least fixpoints are fundamental to program semantics, but they abstract away the recursive structure that generated them. We introduce operator semantics: a semantic intermediate representation between syntax and classical denotational semantics, which treats programs as operators. Abstract compilation is then understood as the act of abstracting such operators. We develop higher-order abstract domains for functions, operators, and programs themselves, in which composition is the key novel primitive, together with a categorical framework for constructing sound, precise, and modular abstract compilers. We instantiate this framework in the context of recurrence-based static cost analysis, developing solver-independent, optimal recurrence extraction techniques for recursive programs over algebraic data types, that support general function unknowns and catamorphic metrics, a broad class of size metrics beyond traditional approaches.

</details>


<div id='cs.CR'></div>

# cs.CR [[Back]](#toc)

### [10] [VUPER: Verified ASN.1 UPER Parser](https://arxiv.org/abs/2608.09094)
*Xiaotian Zhou,Kai Tu,Ali Ranjbar,Yilu Dong,Gang Tan,Syed Rafiul Hussain*

Main category: cs.CR

TL;DR: VUPER是一个用于生成可验证的ASN.1 UPER解析器的框架。它形式化了位精确解析器的概念，验证了解析器和序列化器的往返一致性，实现了遵循UPER规范的组合子，并开发了将ASN.1定义编译为已验证解析器的编译器。通过在5G和V2X协议上测试11个开源及商业解析器，VUPER发现了20种不一致性，并展示了利用这些漏洞的具体攻击。


<details>
  <summary>Details</summary>
Motivation: ASN.1 UPER编码规则广泛应用于蜂窝网络和V2X通信等安全关键领域，但这些底层基础设施中解析器的正确性和安全性至关重要。现有解析器可能存在与UPER标准不一致的实现，导致安全隐患。

Method: 1) 形式化定义位精确解析器的概念，识别保证解析器与序列化器往返一致性的性质（考虑前后兼容性）；2) 实现并验证ASN.1基本类型和结构的解析器/序列化器组合子；3) 开发将ASN.1定义编译为已验证解析器的编译器；4) 构建以VUPER解析器为测试预言机的动态测试框架。

Result: 在5G和V2X通信协议上测试了7个开源和4个商业ASN.1解析器，发现20种不一致性，展示了更严格的UPER标准合规性，并证明了利用解析器漏洞可实现具体攻击。

Conclusion: VUPER能够有效发现现有ASN.1 UPER解析器中的不一致性和安全漏洞，为安全关键领域的基础通信设施提供了更可靠的解析器生成方案。

Abstract: ASN.1 is a widely used interface description language, and UPER (Unaligned Packed Encoding Rules) is one of its key encoding rules, particularly popular in security-critical domains such as cellular networks and vehicle-to-everything (V2X) communication. To ensure the correctness and security of this foundational infrastructure, we present VUPER, a framework for generating verified ASN.1 UPER parsers. We first formalize the notion of a bit-precise parser and identify properties that prove round-trip consistency for parsers and serializers, while accounting for ASN.1 features such as backward/forward compatibility. We then implement and verify parser and serializer combinators for ASN.1 basic types and structures, while adhering to the UPER specification. We also develop a compiler that translates ASN.1 definitions into verified parsers. Finally, we develop a dynamic testing framework using the VUPER parser as a test oracle. To empirically evaluate our approach, we test 7 open-source and 4 commercial ASN.1 parsers using 5G and V2X communication protocols. VUPER uncovers 20 types of inconsistencies in popular parsers and demonstrates stricter compliance with ASN.1 UPER standards. Additionally, we demonstrate concrete attacks by exploiting these parser vulnerabilities.

</details>


### [11] [Renaming or Tightness: Enforcing Disjunctive Information Flow Policies](https://arxiv.org/abs/2608.09120)
*Xin Xu,Siru Tao,Kaizhen Tan*

Main category: cs.CR

TL;DR: 本文为Hunt和Sands提出的信息量子（quantale of information）构建了流敏感的析取策略类型系统族，揭示了通用类型对象的二分性，并分析了不同语义下的精度差异及执行后果。


<details>
  <summary>Details</summary>
Motivation: 析取策略（disjunctive policy）允许一个值最多依赖两个秘密之一，这类策略不构成格结构，Hunt和Sands引入了信息量子来赋予其语义，但执行层（enforcement layer）尚未解决。本文旨在填补这一空白。

Method: 在程序变量的自由交换量子上构建了流敏感类型系统族，证明了单调重命名、典范推导、主类型、内部完备性等性质对该族成立。同时比较了自由对象与带幂等生成元的自由对象上的精度差异，并提出了将特化推迟到判断层（judgement level）以恢复精度的方案。

Result: 发现通用类型对象分裂为二：在自由交换量子上机制对每个策略都成立，而在幂等生成元对象上认证上界更精确且仍然健全。独立属性族内部无法缩小这一差距。在伦理墙（ethical-wall）和秘密共享标签下，对析取源的第二次读取会驱动所有此类证书到无保证状态，导致满足策略的程序被拒绝。通过将特化推迟到判断层可恢复精度，得到的读出映射是最小健全的保并映射。

Conclusion: 析取策略的类型系统执行需要在通用类型对象的两种形式之间做出权衡：自由交换量子提供通用性但精度较低，带幂等生成元的对象提供更高精度但存在被拒绝的风险。推迟特化到判断层是恢复精度的有效途径。

Abstract: A disjunctive policy allows a value to depend on at most one of two secrets and never on both: an analyst may consult one client's file or the other's, a share of a split secret may be released but not its sibling. Such policies are not lattice-shaped, and Hunt and Sands introduced the quantale of information to give them a semantics, leaving the enforcement layer open. We build the flow-sensitive type system family that the quantale calls for, and show that the object which makes such families useful, the universal type object from which every member specialises, splits in two, with a consequence for enforcement. Over the free commutative quantale on the program variables the whole mechanism survives for every policy: monotone renaming, canonical derivations, principal typings, internal completeness. Over the free object with idempotent generators the certified bound is strictly more precise and still sound, because it records that two reads of one source honour one disjunct. The gap cannot be closed from inside the independent-attribute family: no mechanism of that shape whose labelling maps support monotone renaming certifies a bound more precise than the first, and for principal typings under generator-exact homomorphic specialisation the two coincide. Under the ethical-wall and secret-sharing labels the second read of a disjunctive source therefore drives every such certificate to no guarantee, and programs that satisfy the policy are rejected. The literal transcription of the lattice-era object is no escape either: it is a further quotient that loses branch disjunction. Precision is recovered by deferring specialisation to the judgement level, and the resulting read-out map is the least sound join-preserving one.

</details>


<div id='cs.FL'></div>

# cs.FL [[Back]](#toc)

### [12] [Certified Split Windows for Parallel Lexing: Recovering Boundaries Where No Byte Certifies](https://arxiv.org/abs/2608.09761)
*Nicklas Nidhögg*

Main category: cs.FL

TL;DR: 本文提出从"单字节切分点"推广到"有界窗口"的认证机制，使得并行词法分析器在字符串、注释或空白串等传统token集合下也能找到可认证的切分位置。通过构建保守的maximal-munch扫描器历史模型、证明其可靠性并精确判定可达性，为每个窗口提供带有起始位置的认证或不可行的证明。


<details>
  <summary>Details</summary>
Motivation: 前人的可控研究中，某些传统token集合（包含字符串、注释或空白串）无法认证任何单字节切分点，这限制了并行词法分析在真实场景中的应用。需要一种更通用的切分机制来克服这一局限。

Method: 将认证对象从单字节推广到有界字节窗口：仅需知道窗口末尾字节所在token的起始位置即可。构建一个保守的maximal-munch扫描器可穿越窗口的可能历史模型，证明其可靠性，并通过穷举该模型可达配置的有限商集来精确判定可达性。所有结果以认证窗口（含起点）或模型无解证明的形式给出。

Result: 在400个随机token集合样本中，95个不可空集合里91个获得了有见证的窗口，零次不确定搜索；前人研究中的每个精确空行都获得了2到4字节的有见证窗口。Rewind-stress测试运行了1,079,392次执行，零分歧。分析仅在自动机构造后运行一次，仅依赖编译后的表，无需输入。

Conclusion: 窗口化认证方法在保持可靠性的前提下，显著扩展了可认证切分点的适用范围，解决了单字节认证在常见token集合下的失效问题，同时保持了分析的高效率和确定性。

Abstract: A certified split point lets a parallel lexer cut unlexed input at a single byte with the serial token stream provably preserved, but several conventional token sets in the predecessor's controlled study certify no byte once string, comment, or whitespace-run forms are included (arXiv:2608.03473). We generalize from a byte to a bounded window: a byte string after which the position where the current token began is known, regardless of surrounding context. We certify the directly usable form of that recovery: the token covering the window's final byte begins at the reported origin. The certificate is conditional on occurrence and may be vacuous; every applicability figure counts only windows carrying an asserted completely tokenizable occurrence witness. We give a conservative model of a maximal-munch scanner's possible histories across a window, prove it sound, and decide reachability in that model exactly by exhausting a finite quotient of its reachable configurations, so every answer of the unbudgeted procedure is either a certified window with its origin or a proof that the model admits none. Within the stated flat, non-nullable, completely-tokenizable scope, model-positive answers are semantic certificates; negatives are relative to the conservative model, which deliberately refuses some windows a greedy scanner would allow. In a sample of 400 random token sets, 91 of the 95 non-nullable sets certifying no byte gain a witnessed window, with zero inconclusive searches, and every exact-empty row of the predecessor's study gains a witnessed window of two to four bytes. Rewind-stress rows exercised 1,079,392 executions that scanned through the window and contained at least one rewind, with zero disagreements against the shipped scanner. The analysis runs once after automaton construction, using only the compiled tables and no input.

</details>


<div id='stat.ML'></div>

# stat.ML [[Back]](#toc)

### [13] [LazyHMC: Hamiltonian Monte Carlo Simulation for Lazy, Infinite Dimensional Probabilistic Programs](https://arxiv.org/abs/2608.08588)
*Maria-Nicoleta Crăciun,C. -H. Luke Ong,Tom Schrijvers,Sam Staton*

Main category: stat.ML

TL;DR: 本文针对Haskell惰性求值下的概率编程框架，提出了面向无穷维参数空间的梯度基Hamiltonian Monte Carlo（HMC）新方法。通过引入"分段解析且在圆柱解析划分下"（PACAP）的新概念，证明了惰性定义的程序中似然函数的梯度是有限支撑的；同时开发了多种HMC变体及No-U-Turn Sampler，使其能在隐式无穷维参数空间中高效运行，并在高斯混合聚类、随机游走和泊松过程变点回归等实验中验证了有效性。


<details>
  <summary>Details</summary>
Motivation: 标准HMC需要梯度且仅适用于有限维参数空间，但Haskell的惰性求值允许概率程序表达随机过程等非参数贝叶斯模型，其背后是隐式无穷维空间。如何在这种无穷维设定下进行有效的梯度基HMC推断，是一个尚未解决的关键问题。

Method: 1) 提出PACAP（piecewise analytic under cylindrical analytic partition）新概念，用于分析惰性无穷维程序的可微性，证明似然函数梯度具有有限支撑性；2) 开发适用于无穷维参数空间的多种HMC变体及No-U-Turn Sampler，利用惰性求值保证算法的产出性（productivity）。

Result: 实验涵盖高斯混合聚类、随机游走以及带泊松过程变点的分段常数回归，验证了所提出的无穷维HMC方法在这些非参数贝叶斯模型上的可行性和有效性。

Conclusion: 该工作将梯度基HMC推广到了惰性函数式编程语言中的无穷维参数空间，通过PACAP分析理论和惰性求值驱动的采样算法，实现了对非参数贝叶斯模型的高效推断，弥合了HMC与无限维概率编程之间的鸿沟。

Abstract: Hamiltonian Monte Carlo (HMC) is a successful generic inference method in probabilistic programming, but in its ordinary formulation it needs gradients and finite-dimensional parameter spaces. In Haskell, lazy evaluation lets probabilistic programs express stochastic processes and other non-parametric Bayesian models over implicit infinite-dimensional spaces. This paper develops new formulations of gradient-based HMC for this infinite-dimensional setting, via lazy evaluation. For automatic differentiation, we provide an analysis based on a new notion of "piecewise analytic under cylindrical analytic partition" (PACAP), to show that even if a program is infinite-dimensional and defined lazily, the gradient of the likelihood function is finitely supported. For the Monte Carlo method itself, we develop several HMC variants and a No-U-Turn Sampler that operate over the infinite-dimensional parameter space but are still productive because of lazy evaluation. Experiments cover Gaussian mixture clustering, random walks, and piecewise-constant regression with Poisson-process changepoints.

</details>


<div id='cs.CV'></div>

# cs.CV [[Back]](#toc)

### [14] [What Irregularity Costs: CUDA C++, Rust, and Triton on a Hash-Blocked GPU Workload](https://arxiv.org/abs/2608.08287)
*Petr Korolev*

Main category: cs.CV

TL;DR: 本文在非传统基准（开放寻址哈希表TSDF融合）上比较CUDA C++、Rust (cuda-oxide)和Triton三种GPU语言。常规阶段三者性能接近，但非规则阶段Triton慢一个数量级，Rust接近手写CUDA。差距源于语言表达能力限制（Triton的编译期有界探测循环和无掩码的atomic_cas，Rust的GPU作用域原子加载绕过L1缓存），而非指令数差异。Triton的有界探测还导致融合正确性问题——静默丢弃数据块。


<details>
  <summary>Details</summary>
Motivation: 现有GPU语言比较几乎都在规则化、密集线性代数上运行，各工具链表现都好、差异微小。作者希望在不规则工作负载（哈希表探测、数据依赖的每通道深度、竞争散列）上检验语言选择是否仍"几乎免费"，揭示被常规基准掩盖的真实性能差异。

Method: 在三种语言中实现相同的哈希分块TSDF融合内核：CUDA C++（手写基线）、Rust（经NVIDIA cuda-oxide）、Triton。工作负载包含开放寻址哈希表、compare-exchange插入、数据相关的每通道探测深度和竞争散列。通过指令计数、寄存器占用、硬件计数器（L1驻留）等多维度分析性能差距根源。

Result: 常规阶段（截断带遍历+累加）三者性能接近；非规则阶段（探测+插入）Rust接近手写CUDA，Triton慢超过一个数量级。Rust虽指令更少、compare-exchange更少、寄存器更少且占用相同，但L1驻留率低导致更慢（GPU作用域原子加载因SM间一致性需求绕过L1缓存）。Triton的编译期有界探测循环和atomic_cas无掩码导致巨大开销，且在普通负载因子下会造成静默数据丢失的正确性问题。另发现并修复了cuda-oxide的作用域原子操作缺陷（已合并上游）。

Conclusion: 语言选择在常规基准上几乎免费，但在非规则工作上代价高昂。性能差异可归因于语言无法表达的特定结构（而非指令效率比）。Triton的抽象限制了不规则算法表达，甚至引入正确性风险；Rust的问题源于类型正确性要求与硬件缓存一致性现实的冲突。

Abstract: GPU language comparisons are almost always run on tiled dense linear algebra, where every toolchain is good and the differences are small. We implement the same hash-blocked TSDF fusion kernel in CUDA C++, in Rust through NVIDIA's cuda-oxide, and in Triton, and measure it on a workload with the opposite character: an open-addressed hash table with compare-exchange insertion, data-dependent per-lane probe depth, and contended scatter.
  The result is a split. On the regular stage, which walks a truncation band and accumulates, all three languages land within a small factor of each other. On the irregular stage, which probes and inserts, Rust stays close to hand-written CUDA C++ while Triton is more than an order of magnitude slower. Language choice is nearly free on the work that is usually benchmarked and expensive on the work that is not.
  We attribute both gaps to specific things the languages cannot express, not to ratios. Triton's cost follows from a probe loop that must run to a compile-time bound and from tl.atomic_cas taking no mask, which forces a scratch structure with no counterpart in CUDA. Rust's cost was invisible in every instruction count: its kernel issues fewer instructions, fewer compare-exchanges and fewer registers at identical occupancy, yet was slower. Hardware counters located it in L1 residency. A GPU-scope atomic load must be coherent across SMs, no NVIDIA L1 is, so the type-correct way to read a shared location bypasses the cache on every access.
  Triton's bounded probe is also a correctness problem for fusion: at load factors an ordinary depth trajectory reaches, it silently discards blocks and the reconstruction loses patches of surface with nothing reported. We also report a defect found and fixed in cuda-oxide itself, now merged upstream: its scoped atomic load and store could not be called at all in the build mode that produces real kernels.

</details>


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [15] [P$^{3}$: Joint Program-and-Proof Planning for Verified Code Generation](https://arxiv.org/abs/2608.09277)
*Zenan Li,Ziran Yang,Peiyang Song,Zhaoyu Li,Kaiyu Yang*

Main category: cs.AI

TL;DR: 本文提出P³，一种基于LLM的智能体工作流，用于验证代码生成。与传统先写程序再证明的顺序流水线不同，P³先从规范中推导统一的程序与证明计划，再在该共享计划下展开实现和证明脚手架。作者还引入Lean4Commit0基准，从真实软件仓库中提取核心API并翻译为Lean任务。在四个前沿LLM后端上，P³在Verina、AlgoVeri和Lean4Commit0上均取得最高求解率，比强基线提升4.6-11.2个百分点，并将困难子集的API成本降低约40%、时间降低约37%。消融实验证明联合规划比仅规划实现增益3.3-8.3个百分点。


<details>
  <summary>Details</summary>
Motivation: 现有验证代码生成的主流工作流是两阶段解耦的：先综合程序，再尝试证明其正确性。但这种方法在实践中低效且效果不佳——未预判证明需求的程序可能微妙地不正确或结构上难以验证，迫使LLM陷入反复修补代码和证明的脆弱循环。受Dijkstra"程序与其正确性论证应同步开发"思想的启发，作者提出程序与证明应联合规划。

Method: P³采用智能体工作流：第一步从形式规范中推导统一的程序-证明联合计划（program-and-proof plan）；第二步在该共享计划的指导下同时展开实现代码和证明脚手架。此外，作者构建Lean4Commit0基准，从真实软件仓库提取核心API并翻译其需求（包括跨API的关系规范）为Lean任务，以在现实场景中评估验证代码生成。

Result: 在Verina、AlgoVeri和Lean4Commit0三个基准上，使用四个前沿LLM后端，P³在每个基准-模型组合中均取得最高求解率。相比更强基线，在困难子集上求解率提升4.6-11.2个百分点，每任务API成本降低最多约40%，墙钟时间降低最多约37%。消融实验显示，联合规划相比仅规划实现带来3.3-8.3个百分点的额外增益。

Conclusion: 实验结果表明，程序与证明的联合规划显著优于传统的先程序后证明的顺序流水线，不仅提升了解题成功率，还大幅降低了计算成本和推理时间，验证了Dijkstra"程序与正确性论证应协同开发"理念在现代LLM智能体中的有效性。

Abstract: Verified code generation asks a large language model (LLM) to generate both an executable program and a machine-checkable proof that the program meets a formal specification, promising software that is correct by construction. The de facto workflow decouples the two halves of the problem: first synthesize a program, then attempt to prove it correct. We observe that this sequential pipeline can be both ineffective and inefficient in practice. A program generated without anticipating its proof can be subtly incorrect or structurally difficult to verify, forcing the LLM into brittle repair loops that alternate between patching the code and patching the proof. Inspired by Dijkstra's view that a program and its correctness argument should be developed hand in hand, we propose $P^3$, an LLM-based agentic workflow that first derives a unified program-and-proof plan from the specification, then elaborates the implementation and proof scaffold under this shared plan. To evaluate verified code generation in realistic settings, we further introduce Lean4Commit0, a repository-derived, library-level benchmark built by extracting core APIs from real-world software repositories and translating their requirements, including relational specifications across APIs, into Lean tasks. Using four frontier LLM backends, we evaluate $P^3$ on Verina, AlgoVeri, and our Lean4Commit0 benchmark, where it achieves the highest solve rate in every benchmark--model setting. Compared with the stronger baseline, it improves solve rates by 4.6--11.2 percentage points and reduces per-task API cost by up to roughly 40\% and wall-clock time by up to roughly 37\% on the difficult subset of each benchmark. A targeted ablation further shows gains of 3.3--8.3 points over implementation-only planning, isolating the benefit of planning the program and proof jointly.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [16] [LLM-Based Embeddings for Program Analysis and Optimization](https://arxiv.org/abs/2608.07894)
*Calvin Higgins,Marco Alvarez*

Main category: cs.LG

TL;DR: 本文首次将LLMCompiler（一种在大规模中间表示代码上预训练的LLM）生成的程序嵌入应用于程序分析与优化任务。通过将程序切块、独立嵌入并聚合的方式，结合源代码和IR代码嵌入，在算法分类任务上达到1.54%的错误率，较现有最优方法提升12%，并在异构设备映射任务上取得竞争性精度。


<details>
  <summary>Details</summary>
Motivation: 机器学习（尤其是LLM）在程序分析和优化方面展现出巨大潜力，但目前缺乏将LLM预训练的IR代码嵌入直接应用于下游程序分析和优化任务的系统研究。作者希望探索IR代码嵌入在这些任务中的实际效果。

Method: 使用LLMCompiler（在IR代码上大规模预训练的LLM）生成程序嵌入。具体做法是将程序切分为多个块，用预训练LLM独立嵌入每个块，然后将块嵌入聚合成单一的程序嵌入。实验同时利用源代码和IR代码两种嵌入，并比较其组合效果。

Result: 结合源代码和IR代码嵌入后，算法分类任务错误率降至1.54%，比当前最优方法提升12%；在异构设备映射任务上取得有竞争力的精度。

Conclusion: 实验结果表明，使用性能感知的LLM对IR代码进行嵌入训练，可能为代码优化任务带来最优结果。源代码与IR代码嵌入的组合是有效的，为未来代码分析和优化提供了新的方向。

Abstract: Recent advances have highlighted the potential of machine learning, particularly Large Language Models (LLMs), for analyzing and optimizing programs. We present the first application of program embeddings from LLMCompiler---an LLM massively pretrained on intermediate representation (IR) code---to representative program analysis and optimization tasks. We generate program embeddings directly from source and IR code using a simple approach: split programs into chunks, independently embed each chunk with pretrained LLMs, and then aggregate the chunk embeddings into a single program embedding. Our experiments show that combining source and IR code embeddings achieves an error rate of 1.54\% in algorithm classification, a 12\% improvement over the current state-of-the-art, and a competitive accuracy on heterogeneous device mapping. These findings suggest that training a performance-aware LLM for embedding IR code might yield state-of-the-art results in code optimization tasks.

</details>


<div id='cs.CY'></div>

# cs.CY [[Back]](#toc)

### [17] [From Evaluated Models to Evaluation Aids: A Multi-Evidence Study of LLM-Based Difficulty Calibration for Programming Examinations](https://arxiv.org/abs/2608.07523)
*Hongfei Yan,Jiangkai Xiong,Yiqing Li,Chong Chen*

Main category: cs.CY

TL;DR: 本研究将大语言模型从基准评估对象重新定位为考试难度解读的辅助证据源，通过AI解题表现、结构化审阅者评分等方式，结合学生成绩、题目曝光度和在线评测过程数据，为平行班编程考试提供难度参考。实验显示AI与学生在解题通过率上高度相关（rho=0.866），在79道题样本上AI难度与学生通过率呈负相关（rho=-0.871），但模型身份无法认证、单审阅者设计及输出不稳定性构成明确局限，AI难度标尺不可用于个体学生评价或自动调分。


<details>
  <summary>Details</summary>
Motivation: 平行班编程考试的难度差异影响课程评估公平性，传统方法难以客观量化题目难度。作者希望将大语言模型重新定位为辅助证据源而非基准评测对象，以提供外部参考来辅助题目验证、公平性讨论和质量追踪。

Method: 研究采用两阶段方法：(1) 十个人类模型与120名学生同步解答八道期末考试题，计算AI通过率与学生通过率的相关性；(2) 通过可审计的API调用，运行单一结构化审阅者对来自11个平行班期末考试的79道题进行难度评分，并结合题目曝光度折扣、重复题目扰动测试等多种检验方式，同时分析纵向数据结构和CS101入门课程样本作为边界条件验证。

Result: AI通过率与学生通过率正相关（rho=0.866, p=0.0119），解题综合难度指标与学生通过率负相关（rho=-0.905, p=0.0046）。79道题样本中AI难度与题目通过率相关rho=-0.871，与非尝试率相关rho=0.800。但CS101样本中题级相关性降至-0.552，考试级相关性接近零，表明队列构成主导考试级结果。模型身份无法认证及单审阅者设计构成关键局限。

Conclusion: AI证据可作为题目验证、平行班公平性讨论和纵向质量追踪的外部参考，但其使用有明确边界——模型身份无法验证、单审阅者设计、输出不稳定性，因此AI难度标尺绝不能用于个体学生评估或自动成绩调整。在不同课程层次上AI难度的有效性存在差异，入门课程中考官应依赖队列构成等因素而非AI难度。

Abstract: Difficulty differences across parallel-class programming examinations affect the fairness of course assessment. This study repositions large language models from benchmark evaluation targets to auxiliary evidence sources for interpreting exam difficulty, combining AI evidence with aggregated student performance, item exposure, online-judge process data, and teacher interpretation. First, ten models solved an eight-problem final exam synchronously with 120 students: AI pass rate correlated positively with student pass rate (Spearman rho = 0.866, exact p = 0.0119), and a solving-based composite difficulty index correlated negatively with it (rho = -0.905, exact p = 0.0046). A single structured reviewer was then run via auditable API calls on a third-party OpenAI-compatible endpoint whose model label (gpt-5.6-sol) cannot authenticate an official OpenAI upstream model; call metadata and raw responses are archived. Across 79 problems from 11 parallel-class final exams, AI overall difficulty correlated with problem-level pass rate at rho = -0.871 and with non-attempt rate at rho = 0.800; in a 26-problem longitudinal Data Structures and Algorithms B sample, the correlations were -0.829 and 0.883. A 106-problem introductory-course (CS101) sample marks the boundary: the problem-level correlation weakened to rho = -0.552, and the exam-level correlation across 16 exams was near zero, with cohort composition dominating exam-level outcomes. Exposure-discount (0-0.40) and duplicate-problem perturbation tests did not change these directions. AI evidence can thus serve as an external reference for problem validation, parallel-class fairness discussion, and longitudinal quality tracking, while the model-identity boundary, single-reviewer design, and review-output instability set explicit limits: AI difficulty scales must not be used for individual student evaluation or automatic grade adjustment.

</details>


<div id='cs.OS'></div>

# cs.OS [[Back]](#toc)

### [18] [Velosiraptor: Code Synthesis for Memory Translation](https://arxiv.org/abs/2608.07966)
*Reto Achermann,Em Chu,Ryan Mehri,Ilias Karimalis,Margo Seltzer*

Main category: cs.OS

TL;DR: Velosiraptor是一个自动生成正确、底层操作系统代码的系统，用于编程机器的内存硬件。它利用软件合成技术，让开发者只需编写内存硬件映射行为的高层描述和OS环境，即可自动生成经过验证的实现代码，可直接链接到操作系统其余部分。


<details>
  <summary>Details</summary>
Motivation: 操作系统配置内存硬件是安全关键任务，但硬件不断演进使得手动适配OS代码变得重复、耗时且易引入破坏安全隔离的细微bug。

Method: 利用软件合成技术，并利用问题的领域特定性来提高合成效率。开发者只需提供内存硬件映射行为的高层规范描述和OS环境描述，工具链自动将其转换为已验证的实现。

Result: 该工具链能够将规范转换为可直接链接到操作系统的verified实现；同一规范也可用于生成硬件组件，支持新翻译机制的研究。

Conclusion: Velosiraptor使OS开发者无需手动编写内存硬件配置代码即可移植OS到新硬件平台，并支持硬件组件的生成，为翻译机制研究开辟新途径。

Abstract: Security is among the top concerns of operating system (OS) developers. A secure runtime environment relies on the OS to correctly configure the memory hardware on which it runs. This is mission-critical as it provides essential security-relevant features and abstractions that ensure the integrity and isolation of untrusted applications running alongside each other. Configuring a platform's memory hardware is not a one-off effort as designers constantly develop new mechanisms for translation and protection with different features and means of configuration. Adapting the OS code to the new hardware is not only a manual, repetitive and time consuming task, it may also introduce subtle, but security critical bugs that break security and isolation guarantees.
  We present Velosiraptor, a system that automatically generates correct, low-level OS code that programs the memory hardware of a machine. Velosiraptor leverages software synthesis techniques and exploits the domain specificity of the problem to make the synthesis process efficient. With Velosiraptor, developers write only a high-level description of the memory hardware's mapping behavior and OS environment.
  The Velosiraptor toolchain transforms this specification into a verified implementation that can be linked directly with the rest of the operating system. Incorporating the OS environment into this process allows porting an OS to new hardware platforms without worrying about writing code to configure the memory hardware. We can also use the same specification to generate hardware components. This enables research in new translation mechanisms, freeing up OS developers from manually writing OS code.

</details>
