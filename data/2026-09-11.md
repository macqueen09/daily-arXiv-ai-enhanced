<div id=toc></div>

# Table of Contents

- [cs.PL](#cs.PL) [Total: 1]
- [cs.HC](#cs.HC) [Total: 1]
- [cs.DC](#cs.DC) [Total: 2]


<div id='cs.PL'></div>

# cs.PL [[Back]](#toc)

### [1] [Certified Panic Mode: Repair-Invariant Error Recovery for Maximal-Munch Lexing](https://arxiv.org/abs/2609.10600)
*Nicklas Nidhögg*

Main category: cs.PL

TL;DR: 本文提出一种有定理保证的panic-mode恢复位置选择方法：恢复点要求所有在其证据处提交扫描的“前缀修复”都认同该 token 边界，并给出严格包含的二分刻画，实现于munch库并做了确定性损坏实验。


<details>
  <summary>Details</summary>
Motivation: 经典panic-mode恢复只是“跳过失败扫描向前”的约定，仅承诺progress和落在某集合内，缺乏对落点边界的普适性保证，可能导致恢复位置不可靠。

Method: 用定理选择恢复位置：位置为sound recovery point，当每个通过证书返回证据提交扫描的前缀修复都在该处放置token边界（complete repair为特例），基于committed-prefix lemma和为其他目的发布的证书（arXiv:2608.03473、arXiv:2608.09761）。证据返回形式将答案与其依据区间配对；搜索为按证据顺序的一次前向遍历，可证明终止；保证按自动机、相对于当前活跃模式。实现于munch词法分析库。

Result: 二分：包含严格成立当且仅当没有修复完成而仍有扫描通过证据提交，由two-token witness实现该情形。确定性损坏实验中，证据在损坏中幸存的每个认证答案在所有恢复动作上通过可执行落地检查，40,885/40,885；首答中5,595个依赖被损坏触及证据，落地1,589次（仅报告不断言）。在16,808次试验中，被发布修复程序标记为beyond repair的13,522次里恢复后缀可完整切分且两个量词可证重合（若标注精确则均为空）；3,286次仍未定。

Conclusion: 该恢复保证可部署：知道扫描器无法检查内容的调用方可判断认证边界能否迁移到其意图的干净输入；方法在munch库中实现，实验表明认证答案可靠，并精确定位了经典恢复与认证恢复的差异边界。

Abstract: Classical panic-mode recovery skips a failed scan forward by convention: it promises progress, and membership in a designated set where one is found, but no repair-universal boundary guarantee for the position it lands on. This paper chooses the resume position by theorem: a position is a sound recovery point when every prefix repair whose scan commits through the certificate's returned evidence places a token boundary there, complete repairs the special case, by a committed-prefix lemma over certificates posted for a different purpose (arXiv:2608.03473, arXiv:2608.09761). The quantifier is strictly the stronger one, and a dichotomy locates the difference exactly: the inclusion is strict precisely when no repair completes while some scan still commits through the evidence, a two-token witness realizing the case. The evidence-returning form pairs each answer with the interval it rests on, which is what makes the guarantee deployable: a caller who knows what the scanner cannot check decides whether the certified boundary transfers to the clean input it intended. The search is one forward walk in evidence order and provably terminates; the guarantee is per-automaton, relative to the active mode. The procedure ships in the munch lexing library, and a deterministic corruption study measures its recovery quality beside the classical skip-one and delimiter conventions. Every certified answer whose evidence survives the damage passes its executable landing check on every recovery move, 40,885 of 40,885; among first answers, the 5,595 resting on evidence the damage reached land 1,589 times, reported and never asserted. On 13,522 of the 16,808 trials the shipped repair routine labels beyond repair, the resumed suffix tokenizes whole and the two quantifiers provably coincide, so both are empty there if those negative labels are exact; 3,286 stay undetermined.

</details>


<div id='cs.HC'></div>

# cs.HC [[Back]](#toc)

### [2] [ShellVis: Sandboxed Live Programming for Shell Scripts](https://arxiv.org/abs/2609.11000)
*Joshua Horowitz,Jeffrey Heer*

Main category: cs.HC

TL;DR: 提出通过沙箱（将副作用限制在真实环境的模拟中）来实现带副作用程序的实时编程，并以ShellVis为例应用于shell脚本，通过文件系统安全覆盖实现逐行反馈。


<details>
  <summary>Details</summary>
Motivation: 实时编程能让程序员在编辑时运行和追踪程序，但对于有潜在有害副作用的程序，实时性会把错误变成灾难，因此需要一种机制在保持实时性的同时避免副作用危害。

Method: 通过沙箱化实现实时编程：将副作用限制在真实环境的模拟中。具体实现为ShellVis，对shell脚本逐行提供运行时行为反馈，并通过文件系统的安全覆盖层对文件操作进行沙箱化。

Result: 定性用户评估表明ShellVis对参与者有帮助，取代了繁琐的现有做法并增强了信心；参与者的反馈还揭示了未来研究方向，尤其是需要弥合执行鸿沟（gulf of execution）与评估鸿沟（gulf of evaluation）。

Conclusion: ShellVis证明沙箱可以将实时编程技术带入许多副作用重要的真实编程场景，是一个可行且有益的案例研究。

Abstract: Live programming provides visibility to programmers by running and tracing programs as they are edited. However, for programs with potentially harmful side effects, liveness can turn mistakes into disasters. We propose enabling live programming in environments with side effects via sandboxing: confining effects to a simulation of the true environment. We apply sandboxed live programming in the challenging context of shell scripting: a ubiquitous and powerful---yet notoriously opaque and error-prone---tool. ShellVis provides line-by-line feedback on a shell script's run-time behavior, with file operations sandboxed via a safe overlay of the file system. A qualitative user evaluation finds ShellVis to be helpful to participants, replacing tedious existing practices and instilling confidence. Participant responses also reveal areas for future research, particularly bridging the gulf of execution alongside the gulf of evaluation. ShellVis serves as a case study of how sandboxing can bring live-programming techniques into the many real-world programming contexts where side effects are important.

</details>


<div id='cs.DC'></div>

# cs.DC [[Back]](#toc)

### [3] [Taming Bitwise Behavior in GPU Kernels with Tensor Core: Black-Box Reconstruction, Compiler Enforcement, and Static Verification](https://arxiv.org/abs/2609.11356)
*Ziteng Yang,Nicholas J. Riasanovsky,Warren Deng,Vivek Sarkar*

Main category: cs.DC

TL;DR: 本文刻画GPU归约与GEMM的位级行为，实现闭源库算术的复现、确定性归约优化及跨厂商位等价静态检查。


<details>
  <summary>Details</summary>
Motivation: ML系统对GPU内核确定性与数值可复现性需求日增，但同一确定性内核仍可能逐位不同。浮点归约顺序、部分和精度、FMA与舍入位置是主因，而tile形状等性能选择会改变算术并破坏批次不变性；固定顺序至多损失20%性能，autotuner又无法识别位等价配置。

Method: 1) 提出GEMM归约顺序描述符（含split-K的K划分），据此对闭源库算术做首个黑盒重建，构建Triton GEMM族；2) 在Triton lowering中强制平衡树归约并引入数据布局优化；3) 开发可靠的静态位等价检查器，覆盖NVIDIA PTX与AMD GCN，并集成进Triton autotuner。

Result: Triton GEMM族在Blackwell与Hopper全部测试用例中匹配cuBLAS；在带融合epilogue的真实LLM形状上匹配或超越torch.compile。数据布局优化使GB300与H100上27个内核中的19个进入自由顺序性能10%以内。检查器可限制autotuner搜索到单个位等价类。

Conclusion: 通过归约顺序描述、平衡树归约与静态位等价检查，可在有限性能代价下实现跨厂商、跨编译器的位级可复现，并约束自动调优空间。

Abstract: Determinism and numerical reproducibility are increasingly required of GPU kernels in machine learning systems, yet deterministic implementations of the same kernel can still differ bit for bit. Floating-point reduction order is the primary cause, alongside partial-sum precision, fused multiply-add operations, and rounding placement. These choices may be hand-coded, selected by a block-level language such as Triton, or hidden inside a closed-source library such as cuBLAS or rocBLAS. A tile shape chosen for performance therefore also determines the arithmetic, potentially breaking batch invariance. Preserving a fixed order can cost up to 20 percent, while an autotuner cannot identify which configurations are bitwise equivalent.
  We characterize the factors determining the bitwise behavior of reductions and general matrix multiplication (GEMM). First, we introduce a descriptor of GEMM reduction order, including the partitioning of K in split-K GEMM. Using it, we perform the first black-box reconstruction of a closed-source library's arithmetic for bit-level correctness. Our family of Triton GEMMs matches NVIDIA cuBLAS in all tested cases on Blackwell and Hopper. For realistic LLM shapes with fused epilogues, it matches or exceeds torch.compile performance. Second, we enforce balanced-tree reduction during Triton lowering and introduce a data-layout optimization that brings 19 of 27 kernels on GB300 and H100 within 10 percent of free-order performance. Third, we develop sound static checkers for bitwise equivalence between compiled GPU kernels, including the first checker spanning NVIDIA PTX and AMD GCN. Integrated into Triton's autotuner, the checker restricts search to a single bit-equivalence class.

</details>


### [4] [GPU-CFR: 80x Faster Counterfactual Regret Minimization by Compiling the Game to Static Dataflow and CUDA Graph Replay](https://arxiv.org/abs/2609.11923)
*Boning Li,Longbo Huang*

Main category: cs.DC

TL;DR: GPU-CFR通过将博弈树编译为静态数据流并利用CUDA Graph Replay，将CFR迭代中的操作序列预先固定，大幅减少框架调度开销，在A100上比最快GPU基线快29.8-80.4倍。


<details>
  <summary>Details</summary>
Motivation: CFR是少数在CPU上仍快于GPU的大规模数值负载。每次迭代在数十亿状态树上执行数百万小规模、相互依赖的gather/scatter操作，GPU上每个kernel仅微秒级，导致kernel启动和框架调度主导运行时间，此前GPU实现均不敌优化CPU代码。

Method: 基于"固定博弈中除数值外一切迭代前已知"的观察，提出GPU-CFR：一次性将博弈编译为静态数据流（扁平边与信息集数组、预计算索引、深度层批处理遍），仅求解器状态随迭代变化；采用静态chance折叠、深度层执行块、双通道reach缓冲区；因形状、索引、缓冲区地址不变，用CUDA Graph Replay录制一次迭代并以单次图启动重放。

Result: A100上跨八款游戏（牌类、骰子、棋类），比同加速器最快先前GPU CFR快29.8-80.4倍；在四款最大游戏上比开源最快CPU实现LiteEFG快14-258倍。编译表示贡献主要优势：8线程CPU无加速器下已比GPU基线快2.2-51.1倍。CPU优化路径逐位复现参考迭代，树构建与图捕获在首次求解内回本。

Conclusion: GPU-CFR通过静态数据流编译与CUDA Graph Replay，在不改变CFR更新规则的前提下，于中大型博弈上击败所有CPU与GPU基线，解决了CFR在GPU上调度开销主导的问题。

Abstract: Counterfactual regret minimization (CFR) is one of the few large numerical workloads that still runs faster on CPUs than on GPUs. Each iteration sweeps a game tree with up to billions of states in millions of small, interdependent gather and scatter steps issued through a generic tree interface. On a GPU every kernel finishes in microseconds, so kernel launches and framework dispatch dominate the run time, and prior GPU implementations have lost to optimized CPU code. We observe that for a fixed game, everything about a CFR iteration except the numerical values is known before the first iteration runs. We propose GPU-CFR, a compiler and runtime built on this observation. It compiles any game once into static dataflow: flat edge and information-set arrays, precomputed indices, and depth-level batched passes fix the entire operation sequence, and only solver state changes between iterations. Static chance folding, depth-level execution blocks, and a dual-lane reach buffer cut the number of framework operations by up to 18.1x. Because shapes, indices, and buffer addresses never change, CUDA Graph Replay records the iteration once and replays it with a single graph launch. On one A100, across an eight-game suite that spans card games, dice games, and board games, GPU-CFR runs 29.8--80.4x faster than the fastest prior GPU CFR on the same accelerator, and 14--258x faster than LiteEFG, one of the fastest open-source CPU implementations, on the four largest games. The compiled representation carries most of that margin: on eight CPU threads with no accelerator it is already 2.2--51.1x faster than the GPU baseline. On the CPU the optimized path reproduces the reference iterates bitwise, and tree construction and graph capture pay for themselves within the first solve. GPU-CFR beats every CPU and GPU baseline on the mid-to-large games of the suite without changing the update rule.

</details>
